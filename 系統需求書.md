# 問卷調查系統需求規格書

## 1. 文件資訊

- **系統名稱：** 問卷調查管理系統
- **文件類型：** 系統需求規格書
- **主要用途：** 多專案問卷建立、指定使用者登入填寫、回答管理及填寫率統計
- **主要資料庫：** Google Sheet
- **檔案儲存：** Google Drive
- **建議開發架構：** GitHub Pages (前端) + Google Apps Script (後端 API) + Google Sheet + Google Drive
- **前端部署網址：** https://kgilife.github.io/survey-system/
---

# 2. 系統目標

建立一套以「問卷專案」為核心的問卷調查系統。

系統需支援：

1. 多位管理員使用不同管理金鑰登入。
2. 每位管理員只能看到及管理自己建立的專案。
3. 每份問卷為一個獨立專案。
4. 每個專案擁有獨立的使用者名單、問卷、回答資料及登入網址。
5. 管理員可透過類似 Google 表單的方式設計問卷。
6. 問卷使用者必須以帳號及密碼登入後才能填寫。
7. 使用者可暫存、送出及再次修改回答。
8. 回答及設定資料以 Google Sheet 作為主要資料庫。
9. 簽名及圖片檔案儲存於 Google Drive。
10. 管理員可查看填寫狀態、統計填寫率及下載資料。

---

# 3. 使用者角色

系統分為以下兩種角色：

## 3.1 管理員

管理員使用「管理金鑰」登入。

每一組管理金鑰代表一位獨立管理員，管理員只能：

- 查看自己建立的專案。
- 新增自己的問卷專案。
- 編輯自己建立的專案。
- 管理自己專案內的使用者。
- 設計自己專案內的問卷。
- 查看及修改自己專案內的回答。
- 查看自己專案的統計資料。
- 下載自己專案的資料。

管理員不得：

- 查看其他管理員的專案。
- 編輯其他管理員的專案。
- 讀取其他管理員專案中的使用者或回答資料。

## 3.2 問卷使用者

問卷使用者透過專案專屬網址登入。

使用者可以：

- 登入指定問卷專案。
- 填寫問卷。
- 暫存回答。
- 送出回答。
- 再次登入並修改已暫存或已送出的回答。
- 在專案截止後登入查看先前回答。

專案截止後，問卷預設改為唯讀：

- 使用者可以查看回答。
- 使用者不可修改或重新送出。
- 管理員仍可從管理後台修改回答。

---

# 4. 系統整體架構

## 4.1 建議技術架構

```text
使用者瀏覽器
    │
    ▼
GitHub Pages (前端網頁託管)
    │
    ▼ (API 呼叫)
Google Apps Script (後端 API)
    │
    ├── 管理員登入及權限驗證
    ├── 問卷專案管理
    ├── 使用者登入驗證
    ├── 問卷設計及填寫
    ├── 暫存與送出
    ├── 統計運算
    └── 檔案上傳
            │
            ├── Google Sheet：主要資料庫
            └── Google Drive：簽名、圖片及匯出檔案
```

## 4.2 系統根目錄

系統需在指定 Google Drive 位置建立根目錄：

```text
問卷調查系統/
├── 系統管理資料/
│   └── 問卷系統主資料庫
│
├── 管理員_A/
│   ├── 專案_001/
│   └── 專案_002/
│
└── 管理員_B/
    ├── 專案_003/
    └── 專案_004/
```

---

# 5. 系統主資料庫

系統需建立一份「問卷系統主資料庫」Google Sheet，用來管理跨專案資料。

建議包含以下工作表：

1. `管理員設定`
2. `專案索引`
3. `系統設定`
4. `系統操作紀錄`

---

## 5.1 管理員設定

建議欄位：

| 欄位 | 說明 |
|---|---|
| admin_id | 管理員唯一識別碼 |
| admin_name | 管理員名稱 |
| admin_key_hash | 管理金鑰雜湊值 |
| admin_folder_id | 該管理員的 Drive 資料夾 ID |
| status | 啟用或停用 |
| created_at | 建立時間 |
| last_login_at | 最後登入時間 |

### 管理金鑰規則

- 系統初始預設管理金鑰為 `03434016`。
- 該金鑰對應第一位預設管理員。
- 後續可新增其他管理員及管理金鑰。
- 每組管理金鑰只能對應一位管理員。
- 管理金鑰不得重複。
- 管理金鑰不可直接以明碼寫在前端程式中。
- 建議以雜湊方式儲存。
- 可停用特定管理員，但不刪除其既有專案資料。

---

## 5.2 專案索引

建議欄位：

| 欄位 | 說明 |
|---|---|
| project_id | 專案唯一識別碼 |
| admin_id | 所屬管理員 |
| project_name | 問卷名稱 |
| project_status | 專案狀態 |
| folder_id | 專案 Drive 資料夾 ID |
| spreadsheet_id | 專案 Google Sheet ID |
| login_url | 使用者登入網址 |
| start_date | 開放日期 |
| end_date | 截止日期 |
| created_at | 建立時間 |
| updated_at | 最後更新時間 |

系統必須以 `admin_id` 控制專案權限。

管理員登入後，只能讀取：

```text
admin_id = 目前登入管理員
```

的專案資料。

---

# 6. 管理員登入

## 6.1 登入方式

管理員登入頁只需輸入：

- 管理金鑰

登入流程：

```text
輸入管理金鑰
    ↓
後端比對管理金鑰
    ↓
取得對應 admin_id
    ↓
建立管理員 Session
    ↓
載入該 admin_id 所屬專案
```

## 6.2 登入安全

系統應具備：

- 管理金鑰後端驗證。
- 登入 Session。
- Session 到期後重新登入。
- 登出功能。
- 連續登入失敗限制。
- 短時間內重複嘗試限制。
- 登入操作紀錄。

登入錯誤時，統一顯示：

```text
管理金鑰錯誤，請重新輸入。
```

不得透露金鑰是否存在、管理員姓名或其他資訊。

---

# 7. 管理員首頁

管理員登入後，進入自己的專案管理首頁。

首頁需顯示：

- 問卷名稱
- 專案代碼
- 專案狀態
- 建立日期
- 開放日期
- 截止日期
- 使用者總人數
- 已暫存人數
- 已送出人數
- 填寫率
- 專案登入網址
- 最後更新時間

## 7.1 專案狀態

專案狀態包含：

- `草稿`
- `開放填寫`
- `停止填寫`
- `已截止`
- `已封存`

## 7.2 專案操作

管理員可執行：

- 新增專案
- 編輯專案
- 複製專案
- 開放填寫
- 停止填寫
- 封存專案
- 刪除專案
- 進入問卷設計
- 管理使用者
- 查看回答
- 修改回答
- 查看統計
- 下載資料
- 複製登入網址
- 產生 QR Code
- 預覽使用者頁面

刪除專案前須二次確認。

若專案已有回答資料，系統需明確警告管理員刪除後的影響。

---

# 8. 新增問卷專案

新增專案時，管理員至少需要設定：

- 問卷名稱
- 問卷說明
- 開放日期
- 截止日期
- 完成後顯示文字
- 是否顯示填寫進度
- 專案狀態

使用者送出後允許修改為系統固定功能，不需要每個專案另外設定。

## 8.1 建立專案時的自動作業

系統新增問卷時，需自動：

1. 產生唯一 `project_id`。
2. 將專案綁定目前登入的 `admin_id`。
3. 在管理員資料夾下建立專案資料夾。
4. 建立專案專屬 Google Sheet。
5. 建立簽名檔資料夾。
6. 建立圖片上傳資料夾。
7. 建立匯出檔案資料夾。
8. 建立專案專屬登入網址。
9. 將專案資料寫入系統的 `專案索引`。
10. 建立專案所需的預設 Sheet。

---

# 9. 專案資料夾結構

每個專案的資料夾結構如下：

```text
專案_{project_id}_{project_name}/
├── 專案資料庫 Google Sheet
├── signatures/
├── uploads/
└── exports/
```

說明：

- `signatures`：使用者簽名檔。
- `uploads`：使用者上傳圖片。
- `exports`：管理員產生的 Excel、CSV 或統計資料。

所有資料夾預設不得公開。

---

# 10. 專案 Google Sheet 結構

每個問卷專案建立一份獨立 Google Sheet。

至少包含：

1. `專案設定`
2. `使用者欄位設定`
3. `問卷使用者設定`
4. `問項設計`
5. `一般選項設定`
6. `連結型選項設定`
7. `使用者回答`
8. `填寫狀態`
9. `附件紀錄`
10. `專案操作紀錄`

---

# 11. 問卷使用者設定

## 11.1 基礎欄位

每個專案的使用者資料至少包含：

| 系統欄位 | 預設顯示名稱 |
|---|---|
| account | 業務員代碼 |
| password | 生日後四碼 |

規則：

- `account` 及 `password` 為固定系統欄位。
- 兩欄不得刪除。
- `account` 不得重複。
- `password` 不得作為統計維度。
- 帳號及密碼不得為空白。
- 管理員可修改兩欄在登入頁上的顯示名稱。
- 修改顯示名稱不得影響原本帳號及密碼內容。

例如管理員可將：

```text
account → 員工編號
password → 身分證末四碼
```

但底層欄位仍維持：

```text
account
password
```

## 11.2 自訂欄位

管理員可新增其他使用者欄位，例如：

- 姓名
- 通訊處
- 單位
- 區部
- 組別
- 職級
- 身分別

每個欄位需記錄：

| 欄位 | 說明 |
|---|---|
| field_id | 欄位唯一識別碼 |
| field_key | 系統欄位名稱 |
| field_label | 畫面顯示名稱 |
| field_order | 顯示順序 |
| field_type | 文字、數字、日期等 |
| statistical_dimension | 是否可作為統計維度 |
| active | 是否啟用 |

## 11.3 新增欄位

新增欄位時：

- 必須填寫標頭。
- 標頭不得與現有欄位重複。
- 不得使用系統保留名稱。
- 系統自動產生固定 `field_id`。
- 修改欄位名稱時，不得改變 `field_id`。
- 新增欄位預設可作為統計維度。

## 11.4 刪除欄位

管理員可以：

- 選擇單一欄位刪除。
- 多選欄位後一次刪除。

限制：

- `account` 不可刪除。
- `password` 不可刪除。
- 刪除前須二次確認。
- 若欄位正被統計設定或其他功能使用，需顯示影響提示。

---

# 12. 使用者資料批次貼上

管理員可從 Excel 或 TXT 複製資料，直接貼到網頁表格中。

資料格式：

```text
帳號[TAB]密碼[TAB]姓名[TAB]通訊處
A001[TAB]0101[TAB]王小明[TAB]台北通訊處
A002[TAB]0202[TAB]李小華[TAB]新北通訊處
```

系統需支援：

- Tab 分隔欄位。
- 換行分隔使用者。
- 第一列作為欄位標頭。
- 無標頭資料直接依現有欄位順序貼上。
- 貼上後預覽。
- 欄位數量檢查。
- 必填欄位檢查。
- 重複帳號檢查。
- 錯誤列標示。
- 確認後才正式寫入 Google Sheet。

遇到重複帳號時，管理員可選擇：

- 跳過重複資料。
- 覆蓋既有資料。
- 僅更新非空白欄位。
- 取消整次匯入。

---

# 13. 問卷設計器

問卷設計操作方式以 Google 表單為主要參考。

## 13.1 基本操作與獨立工具列

問卷設計器頂部提供 4 個獨立操作按鈕：

- `[＋ 區段]`：新增分頁區段。
- `[＋ 標題與說明]` (`heading`)：新增獨立版面標題與說明卡片（不佔用題目編號、無必填設定）。
- `[＋ 圖片說明]` (`image_note`)：新增獨立圖片說明卡片（不佔用題目編號、無必填設定）。
- `[＋ 題目]`：新增問卷填答題目。

管理員可以：

- 新增題目與版面元素。
- 編輯題目與內容說明。
- 複製題目。
- 刪除題目。
- 調整題目與區段順序。
- 設定題目必填。
- 設定題目說明。
- 設定格式驗證。
- 新增區段。
- 調整區段順序。
- 預覽問卷。
- 儲存與自動同步。

每個題目需有固定且唯一的 `question_id`。

即使題目文字被修改，既有回答仍需透過原 `question_id` 正確對應。

## 13.2 題型分類與下拉選單結構

問卷設計器的「題型下拉選單」重整為以下 5 大類別，版面元素（標題與說明、圖片說明）獨立放在工具列，不混入題型下拉選單中：

### 1. 文字與選擇類（基本題型）
- **簡答** (`short`)：單行文字輸入，支援格式驗證設定（無限制 / 數字 / Email / 電話）。
- **詳答** (`paragraph`)：多行文字長答。
- **單選題** (`single`)：單選選項，支援條件跳題。
- **核取方塊** (`checkbox`)：多選核取方塊。
- **下拉式選單** (`dropdown`)：單選下拉選單，支援條件跳題。

### 2. 矩陣與刻度類
- **線性刻度** (`scale`)：評分/量表。
- **單選方格** (`radio_grid`)：矩陣單選（列資料存於 options，欄資料存於 config.cols）。
- **核取方塊格** (`checkbox_grid`)：矩陣多選（列資料存於 options，欄資料存於 config.cols）。

### 3. 日期與時間
- **日期** (`date`)：日期選擇器。
- **時間** (`time`)：時間選擇器。

### 4. 附件與簽名
- **檔案 / 圖片上傳** (`multi_image`)：支援上傳多張圖片與檔案附件。
- **簽名題** (`signature`)：支援手寫線上簽名。

### 5. 專案客製連動區
- **連結型多選題** (`linked_multi`)：依照登入帳號動態顯示可選多選項。
- **連結型簡答題** (`linked_short`)：依照登入帳號動態顯示列項目與簡答填寫。

## 13.3 回答與格式驗證

依題型支援：

- 必填。
- 簡答格式驗證（數字、Email、電話或無限制）。
- 最小 / 最大字數。
- 數字最小值 / 最大值。
- 正規表示式。
- 多選題最少 / 最多選擇項目。
- 日期區間。
- 自訂錯誤訊息。


---

# 14. 分頁與條件式跳題

## 14.1 分頁

管理員可將問卷拆分成多個區段。

每個區段可設定：

- 區段標題。
- 區段說明。
- 區段順序。
- 上一頁按鈕。
- 下一頁按鈕。
- 填寫進度。

## 14.2 條件式跳題

條件式跳題操作方式參考 Google 表單。

管理員可針對單選題或下拉式選單設定：

```text
選擇 A → 前往第 2 區段
選擇 B → 前往第 3 區段
選擇 C → 直接送出問卷
```

規則：

- 每個選項可指定下一個區段。
- 若未設定，依正常區段順序前進。
- 系統需防止跳題規則形成無限循環。
- 因跳題而未顯示的必填題，不納入必填檢查。
- 使用者修改前面答案時，系統需重新計算後續顯示區段。
- 已不再適用的隱藏題回答應清除或標記為無效，不納入正式送出資料。

---

# 15. 特殊題型

除 Google 表單既有功能外，系統需額外支援：

1. 簽名題。
2. 圖片上傳題。
3. 連結型多選題。
4. 連結型簡答題。

---

## 15.1 簽名題

使用者可透過：

- 手機觸控。
- 平板觸控。
- 電腦滑鼠。

在畫面上直接簽名。

畫面需提供：

- 清除。
- 復原。
- 重簽。
- 確認簽名。

簽名確認後：

1. 將簽名轉換為 PNG 圖檔。
2. 上傳至專案的 `signatures` 資料夾。
3. 在 `附件紀錄` 儲存檔案資料。
4. 在使用者回答中記錄附件 ID。

檔名格式：

```text
{project_id}_{account}_{question_id}_{timestamp}.png
```

使用者重新簽名時：

- 新簽名直接取代原回答。
- 不保留舊簽名版本。
- 舊檔可刪除或標記為無效。

---

## 15.2 圖片上傳題

圖片上傳題需允許一次上傳多張圖片。

管理員可設定：

- 最多上傳張數。
- 是否必填。
- 允許的檔案格式。

預設允許格式：

- JPG
- JPEG
- PNG
- HEIC
- WEBP

限制：

- 單一檔案最大 5 MB。
- 每一張圖片皆需獨立檢查檔案大小。
- 系統需檢查副檔名及 MIME Type。
- 不可只依副檔名判斷檔案類型。

檔名格式：

```text
{project_id}_{account}_{question_id}_{timestamp}_{sequence}.{extension}
```

使用者修改回答時，可以：

- 新增圖片。
- 刪除已上傳圖片。
- 重新上傳圖片。

刪除圖片後，回答中的附件紀錄需同步更新。

---

## 15.3 連結型多選題

連結型多選題會依照登入帳號顯示不同選項。

例如：

| 帳號 | 可選選項 |
|---|---|
| A | 1、2 |
| B | 2、3 |

A 登入後只能看到：

```text
1、2
```

B 登入後只能看到：

```text
2、3
```

### 資料結構

`連結型選項設定` Sheet 建議包含：

| 欄位 | 說明 |
|---|---|
| question_id | 對應題目 ID |
| account | 使用者帳號 |
| option_value | 選項實際值 |
| option_label | 選項顯示文字 |
| option_order | 顯示順序 |
| active | 是否啟用 |

同一帳號可有多筆選項。

例如：

| question_id | account | option_value |
|---|---|---|
| Q005 | A | 1 |
| Q005 | A | 2 |
| Q005 | B | 2 |
| Q005 | B | 3 |

管理員可透過：

- 網頁逐筆新增。
- Excel 複製貼上。
- TXT Tab 分隔貼上。
- 下載範本後重新匯入。

送出時，後端需再次確認：

- 選項是否屬於該帳號。
- 選項是否屬於該題目。
- 選項是否仍為啟用狀態。

不得只依前端畫面進行驗證。

---

## 15.4 連結型簡答題

連結型簡答題會依照登入帳號顯示不同的列選項，並在旁邊提供簡答方塊讓使用者填寫。
運作方式與「連結型多選題」類似，資料來源皆為該帳號設定的「連結型選項設定」。

---

# 16. 使用者登入頁面

每個問卷專案需有獨立登入網址，例如：

```text
https://系統網址/survey/{project_id}/login
```

登入網址不得直接暴露：

- Google Sheet ID。
- Google Drive Folder ID。
- 管理員 ID。

## 16.1 登入欄位

預設顯示：

- 業務員代碼。
- 生日後四碼。

實際對應：

| 顯示名稱 | 系統欄位 |
|---|---|
| 業務員代碼 | account |
| 生日後四碼 | password |

顯示名稱由管理員設定。

## 16.2 登入驗證

登入時系統需：

1. 確認專案存在。
2. 確認帳號屬於該專案。
3. 驗證帳號及密碼。
4. 建立使用者 Session。
5. 讀取使用者原有回答。
6. 讀取使用者目前填寫狀態。
7. 進入問卷填寫或查看頁面。

登入錯誤統一顯示：

```text
登入資料錯誤，請確認後重新輸入。
```

不得透露帳號是否存在。

---

# 17. 專案開放與截止規則

## 17.1 開放期間

在開放期間內，使用者可以：

- 登入。
- 填寫。
- 暫存。
- 送出。
- 修改已送出的回答。
- 再次送出。

## 17.2 截止後

截止後，使用者仍可：

- 登入。
- 查看先前回答。
- 查看填寫狀態。

截止後，使用者不可：

- 修改回答。
- 暫存新內容。
- 重新送出。

畫面需顯示：

```text
本問卷已截止，目前僅能查看先前填寫內容。
```

管理員仍可在後台修改回答。

---

# 18. 問卷填寫頁面

填寫頁面需顯示：

- 問卷名稱。
- 問卷說明。
- 使用者識別資訊。
- 問卷題目。
- 填寫進度。
- 目前填寫狀態。
- 最後暫存時間。
- 暫存按鈕。
- 送出按鈕。
- 登出按鈕。

使用者再次登入時：

- 帶入已暫存回答。
- 帶入已送出回答。
- 顯示最後更新時間。
- 顯示目前為暫存或已送出。

---

# 19. 暫存功能

使用者點擊「暫存」時：

- 不檢查必填題。
- 儲存目前已填內容。
- 更新狀態為 `已暫存`。
- 更新最後暫存時間。
- 顯示暫存成功訊息。

訊息例如：

```text
已暫存，您可以稍後再次登入繼續填寫。
```

---

# 20. 送出功能

使用者點擊「送出」時，系統需：

1. 依條件式跳題結果確認實際顯示題目。
2. 檢查所有顯示中的必填題。
3. 檢查回答格式。
4. 檢查圖片大小及格式。
5. 檢查簽名是否完成。
6. 驗證連結型選項權限。
7. 將回答寫入 Google Sheet。
8. 更新填寫狀態為 `已送出`。
9. 記錄首次送出時間。
10. 記錄最後送出時間。
11. 顯示明確的送出成功畫面。

送出成功後，頁面必須明確顯示：

```text
已送出
```

並可搭配顯示：

```text
您的問卷已成功送出。
在問卷截止前，您仍可再次登入修改回答。
```

若必填題未完成：

- 不得送出。
- 標示所有未完成題目。
- 自動定位到第一個未完成的必填題。

---

# 21. 送出後修改

使用者送出後仍可修改回答。

修改流程：

```text
再次登入
    ↓
載入先前已送出的回答
    ↓
修改內容
    ↓
再次送出
    ↓
直接更新原回答
```

規則：

- 不建立新版本。
- 不保留舊回答內容。
- 直接覆蓋原回答。
- 保留首次送出時間。
- 更新最後送出時間。
- 可記錄修改次數，但不保留每次回答內容。

---

# 22. 多裝置登入

同一帳號可以同時在多個裝置登入。

例如：

- 手機。
- 平板。
- 電腦。

資料儲存規則：

- 以最後成功儲存或送出的內容為準。
- 每次儲存需記錄 `updated_at`。
- 若系統偵測使用者載入頁面後，資料已被其他裝置更新，應提示使用者重新載入，避免直接覆蓋較新的回答。

提示例如：

```text
您的回答已在其他裝置更新，請重新載入最新內容後再儲存。
```

---

# 23. 管理員代為修改回答

管理員可從後台：

- 搜尋使用者。
- 查看使用者回答。
- 修改使用者回答。
- 補上遺漏內容。
- 修改附件。
- 代替使用者送出。
- 將狀態改為暫存或已送出。

限制：

- 管理員只能修改自己專案中的回答。
- 管理員修改後直接覆蓋原資料。
- 不保留舊回答版本。
- 系統需記錄管理員修改操作及時間。
- 操作紀錄不需保存修改前的完整回答內容。

---

# 24. 使用者回答資料結構

為支援動態新增、刪除及調整問項，回答資料建議採長表格式。

`使用者回答` Sheet 建議欄位：

| 欄位 | 說明 |
|---|---|
| answer_id | 回答紀錄 ID |
| account | 使用者帳號 |
| question_id | 題目 ID |
| answer_value | 回答實際值 |
| answer_display | 顯示文字 |
| attachment_ids | 附件 ID |
| status | 暫存或已送出 |
| created_at | 建立時間 |
| updated_at | 最後更新時間 |
| submitted_at | 最後送出時間 |
| updated_by | 使用者或管理員 |

同一個：

```text
account + question_id
```

只保留一組有效回答。

修改時採更新或覆蓋，不新增回答版本。

多選題可使用：

- JSON 陣列儲存。
- 或每個選項一筆資料。

系統內部建議以 JSON 儲存，匯出統計資料時再展開。

---

# 25. 填寫狀態

`填寫狀態` Sheet 每個帳號只保留一筆最新紀錄。

建議欄位：

| 欄位 | 說明 |
|---|---|
| account | 使用者帳號 |
| status | 填寫狀態 |
| first_saved_at | 第一次暫存時間 |
| last_saved_at | 最後暫存時間 |
| first_submitted_at | 第一次送出時間 |
| last_submitted_at | 最後送出時間 |
| last_login_at | 最後登入時間 |
| revision_count | 送出後修改次數 |
| updated_by | 最後修改者 |

狀態包含：

- `未填寫`
- `填寫中`
- `已暫存`
- `已送出`

---

# 26. 附件紀錄

`附件紀錄` Sheet 建議欄位：

| 欄位 | 說明 |
|---|---|
| attachment_id | 附件 ID |
| account | 使用者帳號 |
| question_id | 題目 ID |
| attachment_type | signature 或 image |
| file_name | 檔案名稱 |
| drive_file_id | Drive File ID |
| file_size | 檔案大小 |
| mime_type | MIME Type |
| uploaded_at | 上傳時間 |
| active | 是否為目前有效附件 |

附件不得設定為公開連結。

使用者查看自己的附件時，需由系統驗證權限後提供。

---

# 27. 問卷分享功能

管理員可在專案頁取得：

- 使用者登入網址。
- 複製網址按鈕。
- QR Code。
- 預覽登入頁面。
- 預覽問卷頁面。

分享網址必須導向登入頁面，而不是直接開啟填寫頁面。

---

# 28. 統計功能

## 28.1 總填寫率

「已填寫」定義為狀態為 `已送出`。

計算公式：

```text
COUNT DISTINCT 已送出帳號
────────────────────────
COUNT DISTINCT 專案全部帳號
```

同一帳號即使送出或修改多次，也只能計算一次。

管理頁面需顯示：

- 使用者總人數。
- 尚未登入人數。
- 曾登入人數。
- 未填寫人數。
- 已暫存人數。
- 已送出人數。
- 總填寫率。

## 28.2 自訂統計填寫率

管理員可從「問卷使用者設定」的欄位中選擇統計維度。

可選：

- `account`
- 所有管理員新增的自訂欄位

不可選：

- `password`
- 管理金鑰
- 其他敏感系統欄位

管理員可以選擇單一或多層統計維度，例如：

```text
通訊處
```

或：

```text
通訊處 → 單位
```

或：

```text
通訊處 → 單位 → 職級
```

每組統計公式為：

```text
該群組 COUNT DISTINCT 已送出帳號
────────────────────────────
該群組 COUNT DISTINCT 全部帳號
```

統計結果例如：

| 通訊處 | 單位 | 使用者數 | 已送出人數 | 填寫率 |
|---|---|---:|---:|---:|
| 台北通訊處 | A組 | 20 | 15 | 75% |
| 台北通訊處 | B組 | 10 | 8 | 80% |
| 新北通訊處 | A組 | 15 | 6 | 40% |

管理員可：

- 勾選統計維度。
- 拖曳調整維度順序。
- 篩選特定欄位值。
- 展開或收合下一層。
- 下載統計結果。
- 查看對應未填寫人員名單。

---

# 29. 資料下載

管理員可下載：

- 全部使用者回答。
- 僅已送出回答。
- 僅暫存回答。
- 尚未填寫名單。
- 使用者基本資料。
- 填寫狀態。
- 自訂統計結果。
- 附件清單。

支援格式：

- Excel。
- CSV。

## 29.1 寬表格式

每位使用者一列，每一題一欄。

適合：

- 管理員閱讀。
- Excel 篩選。
- 一般統計分析。

## 29.2 長表格式

每一題回答一列。

適合：

- 資料處理。
- 系統分析。
- 進階統計。

---

# 30. 管理後台頁面架構

每個專案管理頁建議包含：

1. `專案設定`
2. `使用者設定`
3. `問卷設計`
4. `連結型選項`
5. `填寫狀況`
6. `回答資料`
7. `統計分析`
8. `附件管理`
9. `分享設定`
10. `操作紀錄`

---

# 31. 操作紀錄

系統需記錄重要操作。

建議欄位：

| 欄位 | 說明 |
|---|---|
| log_id | 紀錄 ID |
| admin_id | 管理員 ID |
| project_id | 專案 ID |
| operator_type | 管理員或使用者 |
| operator_id | 操作者 ID |
| action | 操作類型 |
| target_type | 操作對象 |
| target_id | 對象 ID |
| description | 操作說明 |
| created_at | 操作時間 |

需記錄：

- 管理員登入。
- 新增專案。
- 修改專案。
- 刪除或封存專案。
- 新增或刪除使用者。
- 修改使用者欄位。
- 修改問卷。
- 問卷開放或停止。
- 使用者登入。
- 使用者暫存。
- 使用者送出。
- 使用者修改回答。
- 管理員修改回答。
- 管理員下載資料。

---

# 32. 權限與安全需求

## 32.1 管理員隔離

所有管理功能都必須在後端驗證：

```text
project.admin_id = current_admin.admin_id
```

不可只透過前端隱藏按鈕控制權限。

## 32.2 專案隔離

不同專案必須隔離：

- 使用者資料。
- 問卷題目。
- 回答資料。
- 附件資料。
- 填寫狀態。
- 統計結果。

使用者修改網址不得存取其他專案。

## 32.3 密碼安全

建議：

- 管理金鑰以雜湊方式儲存。
- 問卷使用者密碼以雜湊方式儲存。
- 前端不得讀取完整帳密名單。
- 所有登入驗證由後端完成。
- Google Sheet 不對問卷使用者開放。
- Google Drive 專案資料夾不對問卷使用者開放。

## 32.4 檔案安全

- 上傳檔案不得設定為公開。
- 檔案網址不得直接暴露 Drive 權限。
- 查看附件前需驗證管理員或使用者權限。
- 系統需限制檔案大小及類型。

---

# 33. 不需要開發的功能

目前明確不需要：

- Email 自動提醒。
- 簡訊自動提醒。
- 填寫完成證明。
- 特定公司網域限制。
- 特定公司網路限制。
- 自動刪除資料。
- 資料保存期限。
- 回答歷史版本。
- 回答修改前後差異紀錄。

---

# 34. 主要操作流程

## 34.1 管理員流程

```text
輸入管理金鑰
    ↓
登入管理後台
    ↓
查看自己的專案
    ↓
新增問卷專案
    ↓
系統建立 Drive 資料夾及 Google Sheet
    ↓
設定使用者欄位
    ↓
貼上使用者帳號資料
    ↓
設計問卷及特殊題型
    ↓
設定分頁及條件式跳題
    ↓
預覽問卷
    ↓
開放填寫
    ↓
分享專屬登入網址
    ↓
查看填寫率及回答
    ↓
必要時代替使用者修改
    ↓
下載資料
```

## 34.2 問卷使用者流程

```text
開啟專案登入網址
    ↓
輸入業務員代碼與生日後四碼
    ↓
驗證登入資料
    ↓
載入先前回答
    ↓
填寫問卷
    ↓
暫存或送出
    ↓
送出後顯示「已送出」
    ↓
截止前可再次登入修改
    ↓
截止後可登入查看，但不可修改
```

---

# 35. 驗收標準

系統至少需通過以下驗收情境：

## 35.1 多管理員

- 使用不同管理金鑰登入後，看到不同的專案。
- 管理員 A 無法存取管理員 B 的專案。
- 即使手動修改網址或 project_id，也無法越權存取。

## 35.2 專案建立

- 新增專案後，自動建立 Drive 資料夾。
- 自動建立專案 Google Sheet。
- 自動建立預設工作表。
- 自動產生專屬登入網址。

## 35.3 使用者設定

- 可修改帳號及密碼的畫面顯示名稱。
- 修改顯示名稱不影響帳密內容。
- 可貼上 Excel 或 TXT 資料。
- `account` 與 `password` 不可刪除。
- 可新增及多選刪除其他欄位。

## 35.4 問卷設計

- 可新增及調整一般 Google 表單式題型。
- 可新增分頁。
- 可設定條件式跳題。
- 可新增簽名題。
- 可新增多圖片上傳題。
- 可新增連結型多選題。

## 35.5 填寫流程

- 使用者可暫存未完成問卷。
- 再次登入可看到原回答。
- 必填題未完成時不可送出。
- 送出後明確顯示「已送出」。
- 送出後可再次修改。
- 修改後直接覆蓋，不保留舊版本。
- 截止後可查看但不可修改。

## 35.6 統計

- 總使用者數採 `COUNT DISTINCT account`。
- 已送出人數採 `COUNT DISTINCT account`。
- 重複送出不會重複計算。
- 可依一個或多個使用者欄位統計。
- `password` 不可選為統計維度。

## 35.7 附件

- 簽名可由手機、平板及滑鼠輸入。
- 簽名成功儲存至專案資料夾。
- 圖片題可上傳多張。
- 單檔超過 5 MB 時不可上傳。
- 非允許格式不可上傳。

---

# 36. 正式開發所需資源

要直接完成整套系統，需要一次提供或確認以下資源。

## 36.1 Google 帳號

需要一個作為系統擁有者的 Google 帳號。

此帳號需具備：

- 建立 Google Drive 資料夾權限。
- 建立及編輯 Google Sheet 權限。
- 建立 Google Apps Script 專案權限。
- 部署 Apps Script Web App 權限。

## 36.2 Google Drive 位置

需指定：

- 系統根目錄要建立在哪一個 Google Drive。
- 是否建立在「我的雲端硬碟」。
- 或建立在特定共用雲端硬碟。

## 36.3 Apps Script 權限

系統首次執行時，需授權：

- 讀取及寫入指定 Google Sheet。
- 建立及管理指定 Google Drive 資料夾。
- 建立及管理上傳檔案。
- 讀取目前執行者或部署資訊。
- 執行 Web App。

## 36.4 部署設定

需要決定：

- Web App 由哪個 Google 帳號執行。
- 哪些人可以開啟 Web App。
- 是否允許未登入 Google 帳號的問卷使用者進入。

由於問卷使用者已有自建帳號及密碼，建議 Web App 設定為：

```text
以系統擁有者身分執行
```

並允許擁有網址的使用者開啟，再由系統本身執行帳密驗證。

實際可用選項仍需依 Google Workspace 管理政策設定。

## 36.5 系統基本設定

開發前需提供：

- 系統名稱。
- 系統首頁標題。
- Logo，若有。
- 主要顯示色。
- 初始管理員名稱。
- 初始管理金鑰。
- 系統根目錄位置。
- 預計使用者語言，目前預設繁體中文。
- 問卷完成後的預設提示文字。

## 36.6 測試資料

建議提供：

- 兩組測試管理金鑰。
- 兩個測試專案名稱。
- 約 5 至 10 位測試使用者。
- 一份測試問卷。
- 一份連結型多選題範例資料。
- 幾張測試圖片。
- 手機及電腦測試環境。

---

# 37. 開發完成定義

「開發完成」應包含：

1. 完整程式碼。
2. 管理員登入頁。
3. 管理後台。
4. 多管理員權限隔離。
5. 問卷專案管理。
6. 使用者管理。
7. Google 表單式問卷設計器。
8. 分頁及條件式跳題。
9. 簽名功能。
10. 多圖片上傳功能。
11. 連結型多選題。
12. 使用者登入頁。
13. 暫存及送出功能。
14. 送出後修改功能。
15. 截止後唯讀功能。
16. 管理員代為修改功能。
17. 填寫率及自訂統計。
18. Excel 及 CSV 下載。
19. Google Sheet 及 Google Drive 串接。
20. 基本權限及資料驗證。
21. 部署說明。
22. 測試及驗收紀錄。