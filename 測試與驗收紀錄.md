# 測試與驗收紀錄

測試日期：2026-08-06

## 重構範圍

- 舊前端頁面、元件、素材與舊後端 API 已全數移除重寫。
- 保留既有 `.env`、`.clasp.json`、`appsscript.json`、Git 遠端與 Google 授權。
- 重構前未提交內容封存於 `stash@{0}`（`pre-rewrite-gemini-2026-08-06`）。

## 自動檢查

- Google Apps Script 六個 `.gs` 檔案：Node CommonJS 語法檢查通過。
- 前端 production build：Vite 8.2.0 建置通過。
- 前端 lint：0 error；僅有非阻斷的 React hooks dependency 提示。
- GAS 正式部署 `GET?action=ping`：HTTP 200，`pong=true`。

## 實際 Google Drive／Sheet 驗收

- 新版主資料庫及系統根目錄：建立成功。
- 預設管理員以需求書預設金鑰登入：成功。
- 建立 `自動驗收專案_20260806`：成功。
- 專案私人 Drive 資料夾、專案 Google Sheet、`signatures`、`uploads`、`exports`：建立成功。
- 建立四題驗收問卷（文字、簽名、多圖、連結型多選）：成功。
- 自訂使用者欄位與問卷結構寫入：成功。

## 待持續觀察

- Google ContentService 在命令列環境對 POST 的 302 重新導向偶爾回傳中繼頁；前端採瀏覽器 `fetch`，部署後仍應以手機與桌面各完成一次完整操作確認實際網路環境。
- 多管理員隔離已在每個後端專案 API 強制檢查 `project.admin_id === session.admin_id`，正式第二組管理員資料建立後可補做雙帳號黑箱驗收。
