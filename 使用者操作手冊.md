# 問卷調查管理系統｜使用者操作手冊

## 2026-08-10 進階功能補充

- 系統根網址現在會直接開啟管理員登入頁，不再顯示額外的入口介紹畫面。
- 圖片說明題現在可直接上傳圖片；圖片選擇、熱點底圖、簽名及填答圖片上傳時都會顯示「上傳中」，完成前請勿重複操作或關閉頁面。
- 數字設定欄可先刪除原數值再輸入新數值；若離開欄位時仍空白，系統才恢復預設值。
- 巢狀選擇題可直接貼上 Excel 多欄資料，一列代表一條路徑，不必逐項建立樹狀結構。
- 條款內容若短到沒有捲軸，可直接勾選；長內容仍須捲到最底。
- 「庫存管理」頁可查看剩餘庫存與最近 1,000 筆異動，並以正負整數手動調整。管理員將已送出回答改回暫存時，系統會自動歸還該帳號占用的庫存。
- 排序題支援拖曳，並保留上下按鈕供鍵盤或觸控操作。
- 圖片選擇題及熱點題可在問卷設計器直接上傳 JPG、PNG 或 WebP（單檔上限 5 MB）。
- 文字螢光筆題會直接以綠底顯示「喜歡」、紅底顯示「反感」的已選文字。
- MaxDiff 可設定每輪選項數及輪數；統計分析頁會顯示相對效用 `(最偏好次數－最不偏好次數)／顯示次數`。
- 地圖定位題可搜尋地址、取得目前位置，並在地圖上點擊或拖曳圖釘微調。
- 統計分析頁會將所有已送出回答的熱點座標疊加顯示。

> 這份手冊寫給第一次使用本系統的人。你不需要懂程式，只要會使用瀏覽器、複製貼上和點按鈕即可。

## 1. 這個系統是做什麼的？

你可以把它想成一個「有帳號密碼的線上問卷箱」：

- **管理員**像老師：建立問卷、出題、發帳號、查看誰交了、下載答案。
- **填寫者**像學生：使用自己的帳號與密碼登入、回答問題、暫存或送出。
- 每份問卷稱為一個「**專案**」。不同專案有不同題目、使用者與分享網址。

## 2. 開始前要準備什麼？

### 管理員需要

1. 系統網址。
2. 一組已註冊的 Email 與密碼；首次使用者可直接在登入頁建立帳號。
3. 若要批次建立使用者，先在 Excel 整理好帳號、密碼與姓名等資料。

### 填寫者需要

1. 管理員提供的問卷專屬網址或 QR Code。
2. 管理員提供的帳號與密碼。

## 3. 管理員最快上手：從零做出第一份問卷

第一次使用時，照下面順序最不容易漏掉事情：

1. 打開系統網址，畫面會直接進入管理員登入頁。
2. 輸入 Email 與密碼後按「**登入**」，或選擇「建立帳號」。
3. 按「**＋ 新增專案**」，輸入問卷名稱。
4. 到「**專案設定**」填問卷說明、日期與送出後訊息，狀態先保留「草稿」。
5. 到「**使用者設定**」建立填寫者帳號。
6. 到「**問卷設計**」新增區段與題目。
7. 到「**分享設定**」按「預覽登入頁」，親自測試一次。
8. 回到「**專案設定**」，把狀態改成「開放填寫」。
9. 到「**分享設定**」複製網址或 QR Code，傳給填寫者。
10. 收件期間看「填寫狀況」；結束後到「回答資料」下載結果。

> **自動儲存很重要：**專案設定、使用者欄位、題目與連結型選項通常在修改約 1 秒後自動儲存。看到「已自動儲存」再離開分頁；若顯示「儲存失敗」，不要關頁，先確認網路後再修改一次。

---

# 管理員操作

## 4. 登入後看到的「你的問卷專案」

每張專案卡片會顯示：

- **專案狀態**：例如草稿或開放填寫。
- **專案 ID**：系統辨認專案用的編號。
- **填寫率**：已送出人數占全部使用者的百分比。
- **已送出／總人數**：例如 `18／25` 代表 25 人中有 18 人送出。
- **最後更新**：最近修改時間。

按鈕說明：

- 「**＋ 新增專案**」：建立一份全新的問卷。
- 「**管理專案**」：進入該問卷的管理頁。
- 右上角「**登出**」：離開管理後台。使用公用電腦時一定要按。

## 5. 管理專案頁的 10 個分頁

| 分頁 | 用途 | 最常做的事 |
|---|---|---|
| 專案設定 | 決定問卷名稱、開放時間和狀態 | 開放或停止問卷 |
| 使用者設定 | 建立可登入的填寫者 | 批次貼上帳號名單 |
| 問卷設計 | 安排區段、題目與選項 | 出題、調整順序、設定必填 |
| 填寫狀況 | 看整體進度 | 下載尚未完成情形 |
| 回答資料 | 看每一題的答案 | 管理員代填、修正、匯出 |
| 統計分析 | 依班級、組別等分類統計 | 比較各組填寫率 |
| 庫存管理 | 查看限量題的剩餘數量與異動 | 手動增減名額、追蹤扣除與歸還 |
| 附件管理 | 查看簽名與上傳檔案紀錄 | 下載附件清單 |
| 分享設定 | 取得填寫入口 | 複製網址、顯示 QR Code |
| 操作紀錄 | 查詢系統操作歷程 | 確認誰在何時做了什麼 |

左上「**← 返回專案**」會回到所有專案的列表。

## 6. 專案設定

### 每個欄位的意思

- **問卷名稱**：填寫者登入後看到的標題，例如「六年一班戶外教學調查」。
- **專案狀態**：控制問卷目前能不能填。
  - `草稿`：還在編輯，不應提供填寫。
  - `開放填寫`：填寫者可以作答與送出。
  - `停止填寫`：暫時關閉。
  - `已截止`：活動已結束。
  - `已封存`：留存舊資料，不再使用。
- **開放日期／截止日期**：設定允許填寫的時間範圍。
- **問卷說明**：告訴填寫者目的、注意事項或預估時間。
- **送出後訊息**：成功送出後顯示，例如「謝謝你，老師已收到！」。
- **顯示填寫進度**：勾選後，填寫頁上方會出現進度條。

### 下方按鈕

- 「**複製專案**」：複製目前專案並另取名稱。適合用去年的問卷製作今年版本。完成後會直接進入新專案。
- 「**刪除專案**」：刪除整份專案。系統會要求你完整輸入問卷名稱確認，之後專案資料夾會移到垃圾桶。這不是刪除單一題目，按之前務必確認。

## 7. 使用者設定

只有名單中的人，才能用自己的帳號與密碼進入問卷。

### 7.1 設定登入欄位名稱

「帳號顯示名稱」和「密碼顯示名稱」只改畫面上的叫法，不會把帳號變成別的資料。例如可以把它們改成「學號」和「生日後四碼」。

### 7.2 自訂欄位

按「**＋ 自訂欄位**」可增加姓名、班級、組別等資料。每列包含：

- **左欄（欄位代碼）**：給系統看的短名稱，建議只用英文字母、數字與底線，例如 `class_name`。建立並匯入資料後不要隨意更名。
- **右欄（顯示名稱）**：給人看的名稱，例如「班級」。
- **統計**：勾選後，此欄位可在「統計分析」中用來分組。
- 「**刪除**」：移除此自訂欄位。

### 7.3 從 Excel 批次貼上使用者

最簡單的方法：

1. 在 Excel 第一列填欄位代碼，例如 `account`、`password`、`name`、`class_name`。
2. 第二列開始放每一位使用者，一人一列。
3. 選取整個表格並複製。
4. 貼進「Excel／TXT 批次貼上」的大文字框。
5. 確認畫面顯示的「可匯入」筆數正確。
6. 選擇遇到重複帳號的處理方式。
7. 按「**確認匯入**」，等到出現「匯入完成」。處理時按鈕會顯示「處理中…」，請勿連按。

範例（欄與欄之間是 Tab）：

```text
account	password	name	class_name
A001	0101	王小明	六年一班
A002	1225	李小華	六年二班
```

重複帳號的四種處理方式：

- **跳過重複**：保留舊資料，不匯入重複那筆。最安全，第一次操作建議選這個。
- **覆蓋既有**：用新資料取代原資料，空白值也可能蓋掉舊值。
- **僅更新非空白**：新資料有填的欄位才更新，空白欄不動。
- **遇重複則取消**：只要發現任何重複，整次匯入就停止，方便先整理名單。

> 第一列最好使用系統代碼 `account`、`password` 與自訂欄位的左欄代碼，不要只寫畫面顯示名稱。沒有帳號的資料列不會被匯入。

### 7.4 名單與下載

- **使用者名單**：查看帳號、個人資料與狀態。
- 「**下載使用者名單**」：下載完整名單。
- 「**下載尚未填寫名單**」：只下載還沒完成的人，方便提醒。

## 8. 問卷設計：先懂「區段」

「區段」就像書的一頁或一個章節。填寫者看完一區，按「下一頁」才會到下一區。至少要保留一個區段。

- 「**＋ 區段**」：新增一頁。把「新區段」改成有意義的名稱，例如「基本資料」。
- 按住區段左側的「**⠿**」可拖曳整個區段；區段內的題目會一起移動。
- 每個區段底部可直接新增題目、標題與說明或圖片說明，新項目會加入該區段。
- 區段旁「**刪除區段**」會先確認；原本屬於它的項目會移到前一個可用區段，並可按「上一步」復原。
- 「**＋ 標題與說明**」：插入只供閱讀的文字，不需回答。
- 「**＋ 圖片說明**」：插入圖片與說明文字，可填圖片網址或直接上傳 JPG、PNG、WebP。
- 「**＋ 題目**」：新增一題，預設為簡答題。

### 8.1 每張題目卡怎麼用

- 第一個文字框：題目，例如「你最喜歡哪種水果？」。
- 題型選單：決定填寫方式。
- 按住題目左側「**⠿**」可在同一區段排序，或拖到其他區段；放開後題目會立即顯示在新位置。
- 區段選單：可不使用拖曳，直接指定題目出現在哪一頁；選擇後題目會移到該區段底部。
- 「**上一步／下一步**」可復原或重做最近的編輯，快捷鍵為 `Ctrl/Command + Z`、`Ctrl + Y` 或 `Command + Shift + Z`。
- 刪除題目或區段後會出現復原提示；編輯器最多保留最近 100 個步驟。
- 「題目說明」：補充提示或例子。
- **必填**：勾選後，沒回答就不能成功送出。
- 「↑／↓」：將題目向前或向後移一格。
- 「**複製**」：複製相似題目，再修改文字可節省時間。
- 「**刪除**」：刪除此題。

### 8.2 題型完整說明

| 題型 | 填寫者怎麼答 | 適合用在 | 管理員要設定什麼 |
|---|---|---|---|
| 簡答 | 輸入一小段文字 | 姓名、學校、短答案 | 可設無限制、數字、Email、電話格式 |
| 詳答 | 輸入較長文字 | 心得、建議、原因 | 題目與說明 |
| 單選題 | 圓點選一個 | 是／否、只能選一種 | 新增選項；可設定選完跳到哪區 |
| 核取方塊 | 方框選多個 | 興趣、可複選項目 | 新增多個選項 |
| 下拉式選單 | 打開清單選一個 | 年級、縣市、選項很多時 | 新增選項；可設定跳題 |
| 線性刻度 | 拉動 1 到 5 的滑桿 | 滿意度、同意程度 | 目前固定以 1～5 操作 |
| 單選方格 | 每一列只能選一欄 | 多個項目各評一個等級 | 左框每行一個「列」，右框每行一個「欄」 |
| 核取方塊格 | 每一列可選多欄 | 多個時段或條件的複選 | 同上，但每列可複選 |
| 日期 | 從日期欄選年月日 | 生日、活動日期 | 題目與說明 |
| 時間 | 選擇時間 | 到達時間、方便聯絡時間 | 題目與說明 |
| 簽名題 | 在畫布上手寫簽名 | 同意書、簽收 | 填寫者須再按「確認簽名」才算完成 |
| 檔案／圖片上傳 | 從裝置選檔案 | 照片、證明文件 | 預設最多 5 個；單檔不可超過 5 MB |
| 連結型多選題 | 每人看到不同的複選項目 | 每人可選的課程或案件不同 | 按「編輯連結型選項」，貼上個人專屬選項 |
| 連結型簡答題 | 每人看到不同的小題並逐一輸入 | 每人負責的項目不同，且各項都要填文字 | 題目的個人專屬項目需先由系統資料設定好 |

#### 選項題怎麼新增

按「**＋ 新增選項**」，把「新選項」改成答案文字。按選項右邊的「✖」可移除它。

#### 單選／下拉的跳題設定

每個選項可指定：

- **繼續下一個區段**：正常往下一頁。
- **前往：某區段**：跳過中間區段。
- **送出表單**：選到此答案後，本區會成為最後一頁。

跳題只能往後跳，請勿設計跳回前面的區段。設定後一定要用「預覽登入頁」逐條測試。

#### 方格題範例

若題目是「請評分每天的午餐」：

- 列選項每行輸入：`星期一`、`星期二`、`星期三`。
- 欄選項每行輸入：`不喜歡`、`普通`、`喜歡`。

#### 連結型多選題範例

按「編輯連結型選項」，每行貼入：`帳號` + Tab + `系統值` + Tab + `顯示文字`。

```text
A001	COURSE01	籃球社
A001	COURSE03	美術社
A002	COURSE02	合唱團
```

這樣 A001 只看到籃球社與美術社，A002 只看到合唱團。按鈕再次點擊會顯示「隱藏連結型選項」。等待「已自動儲存」再離開。

#### 巢狀選擇題：從 Excel 一次貼上

1. 在 Excel 每一欄放一個層級，例如「縣市」、「行政區」、「里別」。
2. 每一列放一條完整路徑。
3. 複製資料列，貼進題目的「巢狀結構」文字框。
4. 確認各欄以 Tab 分開；若手動輸入，也可用 `/` 分隔。

```text
台北市	中正區	黎明里
台北市	大安區	龍安里
新北市	板橋區	新民里
```

填寫者先選縣市，第二層只會顯示該縣市的行政區，後續層級依此類推。若 Excel 有欄位標題，請不要把標題列一起貼入。

## 9. 填寫狀況

這裡顯示：

- **全部使用者**：名單總人數。
- **已暫存**：有保存但還沒正式送出的人數。
- **已送出**：已正式交卷的人數。
- **填寫率**：已送出 ÷ 全部使用者。
- 「**下載填寫狀態 CSV**」：下載每位使用者的填寫狀態。

## 10. 回答資料

表格每一列代表「某位使用者對某一題的回答」，包含帳號、問題、題型、回答、附件、狀態與時間等資訊。

- 「**下載完整回答資料**」：下載所有人的逐題答案。
- 「**編輯**」：用管理員身分打開該帳號的問卷，可代填或修正答案。
- 代填畫面按「**暫存**」只保存；按「**送出並覆蓋**」會正式覆蓋該使用者的答案。
- 「**← 返回列表**」或「關閉」可回到回答表格。

> 管理員修改的是正式資料。操作前先確認帳號，操作後可到「操作紀錄」查核。

## 11. 統計分析

上方會列出可統計的欄位，例如帳號、班級或組別。勾選後，系統會依所選欄位分組，顯示每組的使用者數、已送出數與填寫率。

例如勾「班級」，就能比較六年一班與六年二班的填寫率；同時勾「班級」和「組別」，分類會更細。

- **總人數**：全部名單人數。
- **曾登入**：至少登入過一次的人數。
- **已送出**：正式交卷人數。
- 「**下載統計結果**」：下載目前所選分類方式的統計表。

若看不到想要的分類，回「使用者設定」，確認該自訂欄位有勾選「統計」。

## 12. 附件管理

顯示由簽名題或檔案／圖片上傳題產生的附件紀錄，包括帳號、題目 ID、類型、檔名、大小和上傳時間。

「**下載附件清單**」下載的是附件的清冊，方便核對誰上傳了什麼；它不是把所有檔案打包下載。

## 12.1 庫存管理

「庫存」是有限數量的名額或品項，例如活動名額、可預約時段或贈品數量。要使用此頁，先到「問卷設計」新增「限量／庫存題」，為每個選項輸入初始庫存並等待自動儲存。

- 填寫者暫存時不扣庫存，正式送出才扣除。
- 修改已送出的數量時，只計算新舊答案的差額。
- 將已送出回答改回暫存時，系統自動歸還庫存。
- 管理員可輸入正數增加、負數減少；每次調整都會留下紀錄。
- 頁面沒有資料通常表示尚未建立庫存題，不是系統故障。

## 13. 分享設定

- 網址欄：填寫者專屬入口。
- 「**複製網址**」：將網址複製到剪貼簿，再貼到 Email 或通訊軟體。
- **QR Code**：填寫者可用手機相機掃描。
- 「**預覽登入頁**」：在新分頁打開填寫入口。正式分享前一定要測試。

分享的是這裡的問卷網址，不是管理後台網址，也不要把後台帳號密碼傳出去。

## 14. 操作紀錄

這裡保留系統操作歷程。資料不對時，可用它確認操作時間、動作與相關內容。此頁只有查看功能。

---

# 填寫者操作

## 15. 登入與作答

1. 打開管理員提供的問卷網址，或掃描 QR Code。
2. 確認畫面上的問卷名稱正確。
3. 輸入自己的帳號與密碼。
4. 按「**登入填寫**」。
5. 依題目回答；紅色 `*` 表示必填。
6. 按「**下一頁**」到下一區；按「**上一頁**」回前一區。
7. 到最後一區後，選擇「暫存」或「送出問卷」。

> 「下一頁」只是換頁，不等於送出。**暫存與送出按鈕只會出現在作答流程的最後一區。**

## 16. 暫存、送出與登出有何不同？

- 「**暫存**」：保存目前答案，狀態仍是「已暫存」，之後可再次登入繼續。
- 「**送出問卷**」：正式交卷。看到「已送出」訊息才算完成。
- 「**登出**」：離開帳號。若還沒按暫存或送出，這次新填的內容可能不會保存。

## 17. 特殊題型怎麼填？

- **簽名題**：在白色區域簽名；「清除」全部重畫，「復原」退回上一筆畫，完成後一定要按「確認簽名」。若已確認但要重畫，按「重簽」。
- **檔案／圖片上傳**：按「選擇檔案」。單檔上限 5 MB，最多數量依題目顯示（預設 5 個）。上傳時會顯示檔名與進度，請等提示消失後再操作；傳錯可在送出前按「刪除」。
- **方格題**：橫向看欄、直向看列；單選方格每列只能選一個，核取方塊格每列可選多個。
- **線性刻度**：拖曳滑桿選擇 1～5。

若問卷已截止，畫面會提示「目前僅能查看先前填寫內容」，此時不能修改或送出。

---

# 建議做法與疑難排解

## 18. 發布前檢查表

- [ ] 問卷名稱與說明都正確。
- [ ] 使用者名單筆數正確，並抽查至少一組帳號密碼。
- [ ] 每題都放在正確區段。
- [ ] 必填題、選項與格式限制設定正確。
- [ ] 跳題的每條路線都測試過。
- [ ] 簽名與檔案上傳題實際測試過。
- [ ] 送出後訊息已填寫。
- [ ] 開放與截止日期正確。
- [ ] 已用「預覽登入頁」完整送出一份測試答案。
- [ ] 最後才把狀態改成「開放填寫」。

## 19. 常見問題

### 管理員無法登入

確認使用的是註冊 Email 與後台密碼，而不是某份問卷的填寫者帳密。忘記密碼時按「忘記密碼」，系統會寄送 15 分鐘內有效的一次性重設連結。特殊系統帳號 `kgi` 不具備可收信信箱，因此不能使用寄信重設。

### 填寫者無法登入

確認網址屬於正確專案、帳號已匯入、大小寫與密碼正確，並確認專案已開放且在允許日期內。

### 修改後不見了

修改設定或題目後要等畫面顯示「已自動儲存」再切換分頁或關閉瀏覽器。若顯示「儲存失敗」，先檢查網路。

### 按送出卻失敗

往上找紅色提示，通常是必填題沒答、格式不符、簽名尚未按「確認簽名」，或檔案仍在處理。

### 為什麼看不到暫存按鈕？

「暫存」只在作答流程最後一區顯示。先按「下一頁」直到最後一區，再按暫存。

### 匯入人數是 0

確認資料用 Tab 分欄、每人各占一列，第一列含 `account` 與 `password`，而且每筆都有帳號。從 Excel 直接複製通常最可靠。

### CSV 中文顯示亂碼

優先用新版 Excel 或 Google 試算表開啟。若用 Excel 匯入，選擇 UTF-8 編碼。

### 畫面一直載入或顯示網路錯誤

確認網路連線，重新整理頁面並再次登入。不要在按鈕顯示「處理中…」時重複點擊。

## 20. 安全提醒

- 後台帳號密碼不能放進公開訊息或問卷說明。依目前業務決策，後台密碼會以明文保存在主資料 Google Sheet，務必嚴格限制該 Sheet 與備份的共用權限。
- 每位填寫者最好使用不同帳號，不要共用。
- 在公用電腦完成後按「登出」，並關閉瀏覽器分頁。
- 下載的名單、回答和附件可能含個人資料，請存放在有權限保護的位置。
- 刪除專案或使用管理員覆蓋答案前，先核對專案名稱與使用者帳號。

---

**一句話記住完整流程：**先建專案 → 匯入使用者 → 設計題目 → 預覽測試 → 開放填寫 → 分享網址 → 追蹤進度 → 下載結果。

## 21. 後台帳號與個人資料

### 21.1 註冊與登入

註冊需輸入 Email、密碼及確認密碼。一般帳號的 Email 必須符合格式且不可重複；密碼不限制字元組成，但不可空白。系統預設特殊管理帳號為 `kgi`，密碼為 `03434016`。

登入後點擊右上角顯示名稱可進入「個人資料」，修改顯示名稱、Email 與密碼。`kgi` 的登入名稱不可修改。

### 21.2 忘記密碼

在登入頁點「忘記密碼」並輸入註冊 Email。系統由部署 Apps Script 的 Google 帳號寄出重設信，連結格式為：

```text
FRONTEND_URL/#/reset-password?token=一次性Token
```

連結有效 15 分鐘且只能使用一次。為避免帳號探測，不論 Email 是否存在，畫面都顯示相同結果。

## 22. 新增題型操作

| 題型 | 管理方式 | 填答方式 |
|---|---|---|
| 圖片選擇 | 每個選項填標題與 HTTPS 圖片網址，可開啟多選 | 點選圖片卡片 |
| 星級評分 | 設定最高 2～10 星 | 點擊星星 |
| 巢狀選擇 | 將 Excel 多欄資料直接貼入「巢狀結構」；一列一條路徑，也可用 `/` 分隔 | 逐層選擇 |
| 項目排序 | 建立選項，可限制取前幾名 | 用上下按鈕排序 |
| 總計分配 | 設定目標總數與單位 | 為各項目輸入數字，總和須符合目標 |
| 限量／庫存 | 在選項旁設定初始庫存 | 輸入數量，正式送出才扣除 |
| 熱點點擊 | 設定底圖網址 | 點擊圖片留下正規化座標 |
| 文字螢光筆 | 輸入固定原文 | 選取文字後標記喜歡或反感 |
| 最大差異法 | 建立同一題組的比較選項 | 各選一個最偏好與最不偏好；多組研究可建立多題 |
| 地圖定位 | 不需 API Key | 使用瀏覽器定位或手動輸入地址、經緯度 |
| 條款同意 | 輸入條款內容 | 長內容需捲到底；內容短而沒有捲軸時可直接勾選 |

庫存只在正式送出時扣除，暫存不保留名額。多人同時送出時，後端會再次檢查；若名額剛好被其他人取走，需重新選擇。修改已送出的數量會依新舊差額扣除或歸還。
