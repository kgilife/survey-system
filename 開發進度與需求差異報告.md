# 開發進度與需求差異報告

本報告總結了目前「問卷調查管理系統」的基礎架構建置進度，並列出目前**與需求書的差異（尚未實作與開發的功能）**。

## 1. 已完成的基礎建設 (Infrastructure Setup)

- **架構規劃修改**：已於 `系統需求書.md` 補上前端將部署在 **GitHub Pages**，並透過 API 呼叫後端 **Google Apps Script** 的技術架構更新。
- **後端 (Google Apps Script) 初始化**：
  - 在 `backend/Setup.gs` 撰寫了能自動在您的 Google Drive (`17uP5-zEjLHbqYzzdYjWMXOWzig_GhsIC`) 建立「問卷系統主資料庫」與相關系統資料夾的腳本。
  - 建立 `backend/Database.gs` 實作對 Google Sheet 資料庫的讀寫共用邏輯。
  - 建立 `backend/Code.gs` 提供給前端呼叫的 API 端點（完成初步的管理員登入與專案列表取得 API）。
- **前端 (GitHub Pages - React) 初始化**：
  - 使用 Vite + React 建置 `frontend/` 專案。
  - 導入了帶有現代感玻璃擬態 (Glassmorphism) 的 `index.css` 全域樣式。
  - 實作了 `App.jsx` 路由系統。
  - 完成了 **AdminLogin.jsx** (管理員登入介面) 與 **AdminDashboard.jsx** (管理員後台框架)。
  - 實作了 `api.js` 用以串接 GAS 後端。

---

## 2. 尚未實作與開發的功能清單 (與需求書之差異)

由於此系統非常龐大（涉及表單設計器、多種特殊題型、使用者作答狀態機、圖檔上傳與圖表統計等），我們目前僅完成了**地基鋪設與登入框架**。以下列出**所有尚未開發**的功能項目：

### 管理員後台 (Admin Dashboard & Form Builder)
- **專案管理**：刪除、複製、封存、狀態切換功能。
- **問卷設計器 (核心功能)**：
  - 尚未實作類似 Google 表單的題目新增/刪除/拖曳排序 UI。
  - 尚未實作條件式跳題、分頁邏輯設定。
  - 尚未實作**簽名題**、**多圖片上傳題**、**連結型選項設定**。
- **使用者名單管理**：Excel 批次貼上使用者資料、修改預設欄位名稱功能尚未實作。
- **資料統計與匯出**：自訂維度統計、圖表呈現及匯出 Excel/CSV 功能皆尚未開發。
- **分享功能**：QR Code 產生與預覽連結分享尚未完成。

### 問卷填寫端 (Survey Responder UI)
- **登入驗證**：使用者輸入「業務員代碼」與「密碼」的登入介面與後端驗證邏輯。
- **填寫與互動邏輯**：
  - 載入問卷題目與依據「條件式跳題」動態顯示欄位的邏輯。
  - **暫存 (Save Draft)** 與 **送出 (Submit)** 功能。
  - 必填驗證、字數與格式檢查。
- **特殊題型前端實作**：
  - 手機/滑鼠**觸控簽名板 (Signature Pad)**。
  - 多圖片預覽與超過 5MB 阻擋上傳。
- **跨裝置與狀態管理**：多裝置登入時的「資料已被覆蓋」提示。

### 後端 API 與 Google 整合
- 處理 Base64 圖片與簽名檔上傳至特定專案 Drive 資料夾的邏輯。
- 建立個別專案的專屬 Google Sheet 資料庫 (目前僅完成了「主資料庫」的建立)。
- 系統操作紀錄的完整追蹤。
