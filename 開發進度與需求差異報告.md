# 開發進度報告 (已全數實作)

本報告總結了目前「問卷調查管理系統」的建置進度。原本與需求書的差異（尚未實作與開發的功能）已於本次更新中**全數實作完成**。

## 1. 基礎建設與架構 (Infrastructure Setup)

- **架構規劃修改**：前端將部署在 **GitHub Pages**，並透過 API 呼叫後端 **Google Apps Script** 的技術架構。
- **後端 (Google Apps Script) 初始化與 API 擴充**：
  - 在 `backend/Setup.gs` 撰寫了能自動在您的 Google Drive 建立「問卷系統主資料庫」與相關系統資料夾的腳本。
  - `backend/Database.gs` 實作了專案獨立資料庫建立邏輯 (一專案一資料庫)，並支援在建立專案時自動新增 Google Sheet。
  - 擴充 `backend/Code.gs`、`backend/ApiAdmin.gs`、`backend/ApiResponder.gs`，提供完整的後端 CRUD API。
  - 在 `appsscript.json` 中加入了明確的 Drive 與 Spreadsheets 權限宣告。
- **前端 (GitHub Pages - React) 初始化**：
  - 使用 Vite + React 建置 `frontend/` 專案，並導入玻璃擬態 (Glassmorphism) 全域樣式。
  - 實作了 `App.jsx` 路由系統與共用的 `api.js`。

---

## 2. 管理員後台 (Admin Dashboard)

- **專案管理介面**：已完成。支援建立專案、切換專案與狀態總覽。
- **問卷設計器 (Form Builder)**：
  - 已實作題目新增/刪除，並整合 `@hello-pangea/dnd` 達成拖曳排序功能。
  - 支援各種題型（簡答、單選、多選、下拉、簽名、多圖上傳）。
  - 已實作「必填」及初步的「條件式跳題邏輯」設定。
- **使用者名單管理 (User Management)**：
  - 已實作 Excel 批次貼上資料（帳號/密碼/姓名）的快速匯入功能。
- **資料統計與匯出 (Statistics)**：
  - 整合 `recharts` 將選擇題結果繪製為長條圖。
  - 整合 `xlsx` 提供「匯出 Excel」功能，可將填寫狀態與所有答案匯出。
- **分享與發布 (Share Settings)**：
  - 整合 `qrcode.react` 自動產生專案專屬的填寫網址與 QR Code。

---

## 3. 問卷填寫端 (Survey Responder UI)

- **登入驗證**：已實作「業務員代碼」與「密碼」的登入介面，並以 SessionStorage 保持登入狀態。
- **填寫與互動邏輯**：
  - 依據後台設定的「條件式跳題」動態隱藏或顯示特定題目。
  - 已實作 **儲存暫存 (Save Draft)** 功能，供下次登入接續填寫。
  - 已實作 **送出問卷 (Submit)** 功能，並包含簡單的必填檢查。
- **特殊題型前端實作**：
  - 整合 `react-signature-canvas` 實作手機/滑鼠皆可使用的**觸控簽名板**。
  - 實作多圖片上傳介面，並加入超過 5MB 自動阻擋的檢查邏輯。

---

## 4. 後續維護與注意事項

- **環境依賴安裝**：若要在本機執行前端，需確認已執行 `npm install @hello-pangea/dnd react-signature-canvas qrcode.react recharts xlsx uuid`。
- **GAS 部署授權**：每次推送新的後端程式碼至 Google Apps Script 並發布時，管理員需重新同意 Drive 與 Spreadsheets 的存取授權。
